$("#language_selector").html("<%= escape_javascript(render 'shared/language_select') %>");
$("#contact_form").html("<%= escape_javascript(render 'homes/form', user: @user) %>");

$("select#language_select").on('change', function(){
  var lang = $(this).val()
  languageChangeAjaxCall(lang)
})

$("form#new_user").on('submit', function(e){
  e.preventDefault();
  sendMessage()
})
